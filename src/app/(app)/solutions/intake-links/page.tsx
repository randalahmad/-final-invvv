import { PageHeader } from "@/components/shared/page-header";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { IntakeLinkForm } from "@/modules/solutions/components/intake-forms";import { listSolutionIntakeLinks } from "@/modules/solutions/intake-service";import { requireUser } from "@/server/authz";
export default async function Page(){const actor=await requireUser();const links=await listSolutionIntakeLinks(actor);return <div className="flex flex-col gap-5"><PageHeader title="روابط حصر الحلول" description="روابط محددة النطاق تسمح بتقديم حل فقط، دون كشف المحفظة أو المستخدمين أو الأعمال الداخلية."/><div className="grid gap-5 lg:grid-cols-2"><Card><CardHeader><CardTitle>إنشاء رابط</CardTitle></CardHeader><CardContent><IntakeLinkForm/></CardContent></Card><Card><CardHeader><CardTitle>الروابط الحالية</CardTitle></CardHeader><CardContent className="space-y-3">{links.map(x=><div key={x.id} className="rounded-xl border p-3"><div className="flex justify-between"><p className="font-semibold">{x.nameAr}</p><Badge variant={x.isActive?"success":"neutral"}>{x.isActive?"نشط":"غير نشط"}</Badge></div><p className="mt-1 text-xs text-muted">{x._count.submissions} حلول مستلمة · ينتهي بـ {x.tokenLast4}</p></div>)}{!links.length?<p className="text-sm text-muted">لا توجد روابط بعد.</p>:null}</CardContent></Card></div></div>}
