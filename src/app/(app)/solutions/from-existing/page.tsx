import{PageHeader}from"@/components/shared/page-header";import{Card,CardContent}from"@/components/ui/card";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{prisma}from"@/server/db";import{requirePermission}from"@/server/authz";
export default async function Page(){await requirePermission("solution.create");const activities=await prisma.innovationActivity.findMany({where:{archivedAt:null},select:{id:true,nameAr:true,type:true,_count:{select:{solutions:true}}},orderBy:{updatedAt:"desc"},take:20});return <div className="space-y-5"><PageHeader title="إضافة من مخرجات سابقة" description="اختيار سجل مصدر موجود وحفظ التتبع إليه دون نسخ المصدر أو إنشاء حل ثانٍ للمخرج نفسه."/><Card><CardContent className="space-y-3 p-5">{activities.map(x=><div key={x.id} className="flex items-center justify-between rounded-xl border p-3"><div><p className="font-semibold">{x.nameAr}</p><p className="text-xs text-muted"><Badge variant="neutral">{x.type}</Badge> · {x._count.solutions?"مرتبط بحل بالفعل":"متاح للنقل"}</p></div><Button size="sm" disabled={x._count.solutions>0}>{x._count.solutions?"مرتبط":"اختيار المخرج"}</Button></div>)}</CardContent></Card></div>}
